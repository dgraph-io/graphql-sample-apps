overwrite: true
schema: "https://discuss-dev.us-west-2.aws.cloud.dgraph.io/graphql"
documents: 
  - 'src/**/*.graphql'
  - 'deploy/dev-setup**/*.graphql'
generates:
  src/types/graphql.ts:
    plugins:
      - typescript
  src/:
    preset: near-operation-file
    presetConfig:
      baseTypesPath: types/graphql
      folder: types
      extension: .ts
    plugins:
      - typescript-operations
      - typescript-react-apollo
    config:
      reactApolloVersion: 3
      withHOC: false
      withHooks: true
      withComponent: false
