query hasWritePermission($username: String!) {
  queryPost {
    category {
      permissions(filter: { role: { eq: WRITER }}) {
        user(filter: { username: { eq: $username } }) {
          username
        }
      }
    }
  }
}
