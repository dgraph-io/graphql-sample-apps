query allPosts {
  queryPost(order: { desc: datePublished }) {
    id
    title
    text
    datePublished
    likes
    category {
      name
    }
    author {
      username
      displayName
      avatarImg
    }
  }
}
